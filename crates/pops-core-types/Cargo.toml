[package]
name = "pops-core-types"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "PoPs core types: the pop_<ts_expiry> unit grammar (cashu/cdk-free, branch-agnostic)"

[dependencies]
thiserror = "1"
serde = { version = "1", features = ["derive"] }
