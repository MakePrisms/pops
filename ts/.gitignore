# Node / Next.js build artifacts for the slice-2 TS packages.
node_modules/
**/node_modules/
.next/
**/.next/
next-env.d.ts
**/next-env.d.ts
*.tsbuildinfo
**/*.tsbuildinfo

# The wasm-pack output (ts/pops-core-wasm/pkg) is generated by ts/build-wasm.sh
# and already carries wasm-pack's own `.gitignore` (`*`); ignore it here too in
# case that file is ever absent.
pops-core-wasm/pkg/
